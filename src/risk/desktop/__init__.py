"""Desktop (native-window) launcher for the chair app.

Packaged Dock-app entry point. Runs the FastAPI app in-process and presents
it in a native macOS WKWebView window via pywebview. See ``__main__``.
"""
